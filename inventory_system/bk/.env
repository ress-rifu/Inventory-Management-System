DB_USER=my_user            # or 'postgres' if you're using the default user
DB_PASSWORD=my_password    # the password you used
DB_HOST=localhost
DB_PORT=5432              # default PostgreSQL port
DB_NAME=my_database
JWT_SECRET=my_jwt_secret  # replace with any secret string